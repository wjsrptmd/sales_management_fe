### 개발 모드로 실행

```
npm run dev
```

### 빌드

```
npm run build
```

### 설치 모듈

```
npm i axios
npm i react-cookie
npm i webpack webpack-cli html-webpack-plugin webpack-dev-server --save-dev
npm i -D @babel/core @babel/preset-react babel-loader @babel/plugin-proposal-class-properties @babel/preset-env
npm i --save-dev source-map-loader
npm i --save-dev url crypto-browserify stream-http https-browserify os-browserify stream-browserify
npm i crypto
npm i mysql2
```
